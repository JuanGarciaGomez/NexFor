package com.project.nexfor.core.ui.theme

import androidx.compose.ui.graphics.Color

// ---- Surfaces (Charcoal / Dark Gray) ----
val Background = Color(0xFF121212) // charcoal background
val Foreground = Color(0xFFEDEDED) // primary text
val Card = Color(0xFF1E1E1E) // cards
val CardElevated = Color(0xFF262626) // elevated card/surface
val Secondary = Color(0xFF262626)
val MutedForeground = Color(0xFF8A8A8A) // secondary text / labels
val Border = Color(0xFF2C2C2C) // borders and inputs

// ---- Primary accent: Electric Amber ----
val Amber = Color(0xFFFFB020)
val OnAmber = Color(0xFF121212)

// ---- Data accent: Neon Teal ----
val Teal = Color(0xFF2DD4BF)
val OnTeal = Color(0xFF062925)

// ---- States ----
val Destructive = Color(0xFFF26666)
val OnDestructive = Color(0xFF121212)